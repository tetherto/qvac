// This file is autogenerated by the hyperschema compiler
// Schema Version: 2
/* eslint-disable camelcase */
/* eslint-disable quotes */
/* eslint-disable space-before-function-paren */

import { c } from 'hyperschema/runtime'

const VERSION = 2

// eslint-disable-next-line no-unused-vars
let version = VERSION

// @tool-sandbox/wire-frame
const encoding0 = {
  preencode(state, m) {
    const flags =
      (m.invocationId ? 1 : 0) |
      (m.generation ? 2 : 0) |
      (m.toolName ? 4 : 0) |
      (m.input ? 8 : 0) |
      (m.value ? 16 : 0) |
      (m.code ? 32 : 0) |
      (m.message ? 64 : 0) |
      (m.component ? 128 : 0) |
      (m.runtime ? 256 : 0) |
      (m.processId ? 512 : 0) |
      (m.protocolVersion ? 1024 : 0) |
      ((version >= 2 && m.configuration) ? 2048 : 0)

    c.string.preencode(state, m.type)
    c.uint.preencode(state, flags)

    if (m.invocationId) c.string.preencode(state, m.invocationId)
    if (m.generation) c.uint.preencode(state, m.generation)
    if (m.toolName) c.string.preencode(state, m.toolName)
    if (m.input) c.json.preencode(state, m.input)
    if (m.value) c.json.preencode(state, m.value)
    if (m.code) c.string.preencode(state, m.code)
    if (m.message) c.string.preencode(state, m.message)
    if (m.component) c.string.preencode(state, m.component)
    if (m.runtime) c.string.preencode(state, m.runtime)
    if (m.processId) c.uint.preencode(state, m.processId)
    if (m.protocolVersion) c.uint.preencode(state, m.protocolVersion)
    if (version >= 2 && m.configuration) c.json.preencode(state, m.configuration)
  },
  encode(state, m) {
    const flags =
      (m.invocationId ? 1 : 0) |
      (m.generation ? 2 : 0) |
      (m.toolName ? 4 : 0) |
      (m.input ? 8 : 0) |
      (m.value ? 16 : 0) |
      (m.code ? 32 : 0) |
      (m.message ? 64 : 0) |
      (m.component ? 128 : 0) |
      (m.runtime ? 256 : 0) |
      (m.processId ? 512 : 0) |
      (m.protocolVersion ? 1024 : 0) |
      ((version >= 2 && m.configuration) ? 2048 : 0)

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.invocationId) c.string.encode(state, m.invocationId)
    if (m.generation) c.uint.encode(state, m.generation)
    if (m.toolName) c.string.encode(state, m.toolName)
    if (m.input) c.json.encode(state, m.input)
    if (m.value) c.json.encode(state, m.value)
    if (m.code) c.string.encode(state, m.code)
    if (m.message) c.string.encode(state, m.message)
    if (m.component) c.string.encode(state, m.component)
    if (m.runtime) c.string.encode(state, m.runtime)
    if (m.processId) c.uint.encode(state, m.processId)
    if (m.protocolVersion) c.uint.encode(state, m.protocolVersion)
    if (version >= 2 && m.configuration) c.json.encode(state, m.configuration)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      invocationId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      generation: (flags & 2) !== 0 ? c.uint.decode(state) : 0,
      toolName: (flags & 4) !== 0 ? c.string.decode(state) : null,
      input: (flags & 8) !== 0 ? c.json.decode(state) : null,
      value: (flags & 16) !== 0 ? c.json.decode(state) : null,
      code: (flags & 32) !== 0 ? c.string.decode(state) : null,
      message: (flags & 64) !== 0 ? c.string.decode(state) : null,
      component: (flags & 128) !== 0 ? c.string.decode(state) : null,
      runtime: (flags & 256) !== 0 ? c.string.decode(state) : null,
      processId: (flags & 512) !== 0 ? c.uint.decode(state) : 0,
      protocolVersion: (flags & 1024) !== 0 ? c.uint.decode(state) : 0,
      configuration: (version >= 2 && (flags & 2048) !== 0) ? c.json.decode(state) : null
    }
  }
}

function setVersion(v) {
  version = v
}

function encode(name, value, v = VERSION) {
  version = v
  return c.encode(getEncoding(name), value)
}

function decode(name, buffer, v = VERSION) {
  version = v
  return c.decode(getEncoding(name), buffer)
}

function getEnum(name) {
  switch (name) {
    default:
      throw new Error('Enum not found ' + name)
  }
}

function getEncoding(name) {
  switch (name) {
    case '@tool-sandbox/wire-frame':
      return encoding0
    default:
      throw new Error('Encoder not found ' + name)
  }
}

function getStruct(name, v = VERSION) {
  const enc = getEncoding(name)
  return {
    preencode(state, m) {
      version = v
      enc.preencode(state, m)
    },
    encode(state, m) {
      version = v
      enc.encode(state, m)
    },
    decode(state) {
      version = v
      return enc.decode(state)
    }
  }
}

const resolveStruct = getStruct // compat

export { resolveStruct, getStruct, getEnum, getEncoding, encode, decode, setVersion, version }
