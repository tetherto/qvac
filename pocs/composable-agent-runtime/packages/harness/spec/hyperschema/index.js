// This file is autogenerated by the hyperschema compiler
// Schema Version: 4
/* eslint-disable camelcase */
/* eslint-disable quotes */
/* eslint-disable space-before-function-paren */

import { c } from 'hyperschema/runtime'

const VERSION = 4

// eslint-disable-next-line no-unused-vars
let version = VERSION

// @harness/wire-frame
const encoding0 = {
  preencode(state, m) {
    const flags =
      (m.runId ? 1 : 0) |
      (m.model ? 2 : 0) |
      (m.messages ? 4 : 0) |
      (m.text ? 8 : 0) |
      (m.name ? 16 : 0) |
      (m.args ? 32 : 0) |
      (m.result ? 64 : 0) |
      (m.metrics ? 128 : 0) |
      (m.message ? 256 : 0) |
      ((version >= 2 && m.traceId) ? 512 : 0) |
      ((version >= 2 && m.component) ? 1024 : 0) |
      ((version >= 2 && m.runtime) ? 2048 : 0) |
      ((version >= 2 && m.instanceId) ? 4096 : 0) |
      ((version >= 2 && m.processId) ? 8192 : 0) |
      ((version >= 2 && m.contract) ? 16384 : 0) |
      ((version >= 2 && m.protocolVersion) ? 32768 : 0) |
      ((version >= 2 && m.capabilities) ? 65536 : 0) |
      ((version >= 2 && m.buildVersion) ? 131072 : 0) |
      ((version >= 2 && m.sdkIdentity) ? 262144 : 0) |
      ((version >= 3 && m.error) ? 524288 : 0) |
      ((version >= 4 && m.progress) ? 1048576 : 0)

    c.string.preencode(state, m.type)
    c.uint.preencode(state, flags)

    if (m.runId) c.string.preencode(state, m.runId)
    if (m.model) c.string.preencode(state, m.model)
    if (m.messages) c.json.preencode(state, m.messages)
    if (m.text) c.string.preencode(state, m.text)
    if (m.name) c.string.preencode(state, m.name)
    if (m.args) c.json.preencode(state, m.args)
    if (m.result) c.json.preencode(state, m.result)
    if (m.metrics) c.json.preencode(state, m.metrics)
    if (m.message) c.string.preencode(state, m.message)
    if (version >= 2 && m.traceId) c.string.preencode(state, m.traceId)
    if (version >= 2 && m.component) c.string.preencode(state, m.component)
    if (version >= 2 && m.runtime) c.string.preencode(state, m.runtime)
    if (version >= 2 && m.instanceId) c.string.preencode(state, m.instanceId)
    if (version >= 2 && m.processId) c.uint.preencode(state, m.processId)
    if (version >= 2 && m.contract) c.string.preencode(state, m.contract)
    if (version >= 2 && m.protocolVersion) c.uint.preencode(state, m.protocolVersion)
    if (version >= 2 && m.capabilities) c.json.preencode(state, m.capabilities)
    if (version >= 2 && m.buildVersion) c.string.preencode(state, m.buildVersion)
    if (version >= 2 && m.sdkIdentity) c.json.preencode(state, m.sdkIdentity)
    if (version >= 3 && m.error) c.json.preencode(state, m.error)
    if (version >= 4 && m.progress) c.json.preencode(state, m.progress)
  },
  encode(state, m) {
    const flags =
      (m.runId ? 1 : 0) |
      (m.model ? 2 : 0) |
      (m.messages ? 4 : 0) |
      (m.text ? 8 : 0) |
      (m.name ? 16 : 0) |
      (m.args ? 32 : 0) |
      (m.result ? 64 : 0) |
      (m.metrics ? 128 : 0) |
      (m.message ? 256 : 0) |
      ((version >= 2 && m.traceId) ? 512 : 0) |
      ((version >= 2 && m.component) ? 1024 : 0) |
      ((version >= 2 && m.runtime) ? 2048 : 0) |
      ((version >= 2 && m.instanceId) ? 4096 : 0) |
      ((version >= 2 && m.processId) ? 8192 : 0) |
      ((version >= 2 && m.contract) ? 16384 : 0) |
      ((version >= 2 && m.protocolVersion) ? 32768 : 0) |
      ((version >= 2 && m.capabilities) ? 65536 : 0) |
      ((version >= 2 && m.buildVersion) ? 131072 : 0) |
      ((version >= 2 && m.sdkIdentity) ? 262144 : 0) |
      ((version >= 3 && m.error) ? 524288 : 0) |
      ((version >= 4 && m.progress) ? 1048576 : 0)

    c.string.encode(state, m.type)
    c.uint.encode(state, flags)

    if (m.runId) c.string.encode(state, m.runId)
    if (m.model) c.string.encode(state, m.model)
    if (m.messages) c.json.encode(state, m.messages)
    if (m.text) c.string.encode(state, m.text)
    if (m.name) c.string.encode(state, m.name)
    if (m.args) c.json.encode(state, m.args)
    if (m.result) c.json.encode(state, m.result)
    if (m.metrics) c.json.encode(state, m.metrics)
    if (m.message) c.string.encode(state, m.message)
    if (version >= 2 && m.traceId) c.string.encode(state, m.traceId)
    if (version >= 2 && m.component) c.string.encode(state, m.component)
    if (version >= 2 && m.runtime) c.string.encode(state, m.runtime)
    if (version >= 2 && m.instanceId) c.string.encode(state, m.instanceId)
    if (version >= 2 && m.processId) c.uint.encode(state, m.processId)
    if (version >= 2 && m.contract) c.string.encode(state, m.contract)
    if (version >= 2 && m.protocolVersion) c.uint.encode(state, m.protocolVersion)
    if (version >= 2 && m.capabilities) c.json.encode(state, m.capabilities)
    if (version >= 2 && m.buildVersion) c.string.encode(state, m.buildVersion)
    if (version >= 2 && m.sdkIdentity) c.json.encode(state, m.sdkIdentity)
    if (version >= 3 && m.error) c.json.encode(state, m.error)
    if (version >= 4 && m.progress) c.json.encode(state, m.progress)
  },
  decode(state) {
    const r0 = c.string.decode(state)
    const flags = c.uint.decode(state)

    return {
      type: r0,
      runId: (flags & 1) !== 0 ? c.string.decode(state) : null,
      model: (flags & 2) !== 0 ? c.string.decode(state) : null,
      messages: (flags & 4) !== 0 ? c.json.decode(state) : null,
      text: (flags & 8) !== 0 ? c.string.decode(state) : null,
      name: (flags & 16) !== 0 ? c.string.decode(state) : null,
      args: (flags & 32) !== 0 ? c.json.decode(state) : null,
      result: (flags & 64) !== 0 ? c.json.decode(state) : null,
      metrics: (flags & 128) !== 0 ? c.json.decode(state) : null,
      message: (flags & 256) !== 0 ? c.string.decode(state) : null,
      traceId: (version >= 2 && (flags & 512) !== 0) ? c.string.decode(state) : null,
      component: (version >= 2 && (flags & 1024) !== 0) ? c.string.decode(state) : null,
      runtime: (version >= 2 && (flags & 2048) !== 0) ? c.string.decode(state) : null,
      instanceId: (version >= 2 && (flags & 4096) !== 0) ? c.string.decode(state) : null,
      processId: (version >= 2 && (flags & 8192) !== 0) ? c.uint.decode(state) : 0,
      contract: (version >= 2 && (flags & 16384) !== 0) ? c.string.decode(state) : null,
      protocolVersion: (version >= 2 && (flags & 32768) !== 0) ? c.uint.decode(state) : 0,
      capabilities: (version >= 2 && (flags & 65536) !== 0) ? c.json.decode(state) : null,
      buildVersion: (version >= 2 && (flags & 131072) !== 0) ? c.string.decode(state) : null,
      sdkIdentity: (version >= 2 && (flags & 262144) !== 0) ? c.json.decode(state) : null,
      error: (version >= 3 && (flags & 524288) !== 0) ? c.json.decode(state) : null,
      progress: (version >= 4 && (flags & 1048576) !== 0) ? c.json.decode(state) : null
    }
  }
}

function setVersion(v) {
  version = v
}

function encode(name, value, v = VERSION) {
  version = v
  return c.encode(getEncoding(name), value)
}

function decode(name, buffer, v = VERSION) {
  version = v
  return c.decode(getEncoding(name), buffer)
}

function getEnum(name) {
  switch (name) {
    default:
      throw new Error('Enum not found ' + name)
  }
}

function getEncoding(name) {
  switch (name) {
    case '@harness/wire-frame':
      return encoding0
    default:
      throw new Error('Encoder not found ' + name)
  }
}

function getStruct(name, v = VERSION) {
  const enc = getEncoding(name)
  return {
    preencode(state, m) {
      version = v
      enc.preencode(state, m)
    },
    encode(state, m) {
      version = v
      enc.encode(state, m)
    },
    decode(state) {
      version = v
      return enc.decode(state)
    }
  }
}

const resolveStruct = getStruct // compat

export { resolveStruct, getStruct, getEnum, getEncoding, encode, decode, setVersion, version }
