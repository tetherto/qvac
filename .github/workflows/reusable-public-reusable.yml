name: General Library CI/CD Workflow
# Shared by multiple projects

on:
  workflow_call:
    inputs:
      debug_mode:
        required: false
        type: boolean
        default: false # DEBUG MODE DISABLED
      workdir:
        required: false
        type: string
        default: "."
    outputs:
      published_version:
        description: "The version that was published to npm"
        value: ${{ jobs.pull-request-event.outputs.published_version }}

env:
  WORKDIR: ${{ inputs.workdir }}

jobs:
  pull-request-event-checks:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || github.event_name == 'pull_request_target' || inputs.debug_mode
    permissions:
      contents: read
      packages: read
    steps:
      - name: Echo Step
        run: |
          echo "Processing ${{ github.event_name }} event"
          echo "PR Number: ${{ github.event.pull_request.number }}"
          echo "PR Title: ${{ github.event.pull_request.title }}"
          echo "repository: ${{ github.event.pull_request.head.repo.full_name }}"
          echo "ref: ${{ github.event.pull_request.head.ref }}"
          echo "workdir: ${{ env.WORKDIR }}"

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch full history for accurate ancestry checks
          token: ${{ secrets.PAT_TOKEN }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Fetch PR head and target branch
        id: fetch_pr
        continue-on-error: true
        run: |
          git fetch origin ${{ github.event.pull_request.base.ref }}:refs/remotes/origin/${{ github.event.pull_request.base.ref }}
          git fetch origin ${{ github.event.pull_request.head.sha }}:refs/pr/head

      - name: Check PR Status with Target Branch
        continue-on-error: true
        id: target_branch_checks
        run: |
          TARGET_SHA=${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA=${{ github.event.pull_request.head.sha }}
          if ! git merge-base --is-ancestor "$TARGET_SHA" "$PR_HEAD_SHA"; then
            echo "::error title=PR is not up to date::This PR is not up to date with target branch"
            exit 1
          fi

      - name: Check for disallowed dependencies
        id: check_dependencies
        continue-on-error: true
        working-directory: ${{ env.WORKDIR }}
        run: |
          if ! jq -e '.dependencies and ( .dependencies | length > 0 )' package.json > /dev/null; then
            echo "↩︎ No runtime dependencies found in package.json. Skipping."
            exit 0
          fi
          deps=$(jq -r '.dependencies, .devDependencies // {} | to_entries[] | .value' package.json)
          if echo "$deps" | grep -Eq '^(git\+https:\/\/github.com|[0-9]+\.[0-9]+\.[0-9]+-(dev|tmp)[^"]*)$'; then
            echo "::error title=Disallowed dependency detected::Do not use git URLs or dev/tmp versions in dependencies"
            exit 1
          fi

      - name: Enable Socket Firewall Guard
        uses: ./.github/actions/sfw-guard
        with:
          firewall-mode: firewall-free
          clear-caches: "false"
          managers: npm,pip,pip3,uv,cargo
          workdir: ${{ env.WORKDIR }}

      - name: Run unit tests
        id: run_unit_tests
        continue-on-error: true
        uses: ./.github/actions/run-lint-and-unit-tests
        with:
          gpr-token: ${{ secrets.GITHUB_TOKEN }}
          npm-token: ${{ secrets.NPM_TOKEN }}
          pat-token: ${{ secrets.PAT_TOKEN }}
          workdir: ${{ env.WORKDIR }}

      - name: Check for errors
        run: |
          failed=0
          if ${{ steps.run_unit_tests.outcome == 'failure' }}; then
            echo "Step - Lint or unit tests failed"
            failed=1
          fi
          if ${{ steps.check_dependencies.outcome == 'failure' }}; then
            echo "Step - Check for disallowed dependencies failed"
            failed=1
          fi
          if ${{ steps.target_branch_checks.outcome == 'failure' }}; then
            echo "Step - Check PR Status with Target Branch failed"
            failed=1
          fi
          if ${{ steps.check_package_name.outcome == 'failure' }}; then
            echo "Step - Check for @tetherto/ prefix in package name failed"
            failed=1
          fi
          if [ $failed -eq 1 ]; then
            exit 1
          fi

  pull-request-event:
    needs: pull-request-event-checks
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' || github.event_name == 'pull_request_target' || inputs.debug_mode
    permissions:
      contents: write
      packages: write
    outputs:
      published_version: ${{ steps.publish-pr.outputs.npm_published_version }}
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Enable Socket Firewall Guard
        uses: ./.github/actions/sfw-guard
        with:
          firewall-mode: firewall-free
          clear-caches: "false"
          managers: npm,pip,pip3,uv,cargo
          workdir: ${{ env.WORKDIR }}

      - name: Call Publish GPR Action
        id: publish-pr
        uses: ./.github/actions/publish-library-to-gpr
        with:
          secret-token: ${{ secrets.GITHUB_TOKEN }}
          pat-token: ${{ secrets.PAT_TOKEN }}
          npm-token: ${{ secrets.NPM_TOKEN }}
          tag: "dev"
          workdir: ${{ env.WORKDIR }}

      - name: Echo published version from PR
        run: |
          echo "Published version from PR: ${{ steps.publish-pr.outputs.npm_published_version }}"

  push-event:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || inputs.debug_mode
    permissions:
      contents: write
      packages: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.PAT_TOKEN }}

      - name: Enable Socket Firewall Guard
        uses: ./.github/actions/sfw-guard
        with:
          firewall-mode: firewall-free
          clear-caches: "false"
          managers: npm,pip,pip3,uv,cargo
          workdir: ${{ env.WORKDIR }}

      - name: Publish to GPR
        id: publish-push
        uses: ./.github/actions/publish-library-to-gpr
        with:
          secret-token: ${{ secrets.GITHUB_TOKEN }}
          pat-token: ${{ secrets.PAT_TOKEN }}
          workdir: ${{ env.WORKDIR }}

      - name: Echo published version from push
        run: |
          echo "Published version from push: ${{ steps.publish-push.outputs.npm_published_version }}"

  debug-print:
    uses: ./.github/workflows/reusable-debugger-reusable.yml
    secrets:
      pat_token: ${{ secrets.PAT_TOKEN }}

  notify-reviewers:
    if: github.event_name == 'pull_request' || github.event_name == 'pull_request_target'
    uses: ./.github/workflows/reusable-automatically-add-reviewers.yaml
    with:
      reviewers: "ignaciolarranaga"
    secrets: inherit
