# Build-only workflow.  Publishing is handled by on-merge-audiogen-ggml.yml
name: "Prebuilds (AudioGen GGML)"

on:
  workflow_dispatch:
    inputs:
      workdir:
        description: "Working directory for the package"
        type: string
        required: false
        default: "packages/audiogen-ggml"
  workflow_call:
    inputs:
      ref:
        description: "ref"
        type: string
      repository:
        type: string
        required: false
        default: "tetherto/qvac"
      workdir:
        description: "Working directory for the package"
        type: string
        required: false
        default: "packages/audiogen-ggml"

permissions:
  contents: read

jobs:
  prebuild:
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    uses: ./.github/workflows/reusable-prebuilds.yml
    with:
      workdir: ${{ inputs.workdir }}
      ref: ${{ inputs.ref }}
      repository: ${{ inputs.repository }}
      artifact-name-prefix: audiogen-ggml-
      linux-extra-packages: pkg-config libx11-dev libxcb1-dev libxkbcommon-dev
      setup-python-on-windows: true
      include-vulkan-sdk: true
    secrets: inherit
