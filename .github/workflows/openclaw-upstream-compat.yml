name: OpenClaw Upstream Compatibility

# Scheduled tripwire for the published OpenClaw integration. This installs the
# latest public OpenClaw and QVAC plugin packages in a fresh project so upstream
# localService, config, model-catalog, and agent-run drift is caught early.

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/openclaw-upstream-compat.yml"
      - "scripts/ci/openclaw-upstream-compat-smoke.sh"
      # The verifiers decide the smoke's verdict, so a change to one is a
      # change to this workflow's behaviour.
      - "scripts/ci/verify-openclaw-agent-output.cjs"
      - "scripts/ci/verify-openclaw-prompt-surface.cjs"
  schedule:
    - cron: "30 7 * * *" # daily 07:30 UTC, after the OpenCode compatibility smoke
  workflow_dispatch:
    inputs:
      attempts:
        description: "Agent attempts before failing (1 for a single-shot debugging run)"
        required: false
        default: "6"
      tool_profile:
        description: "OpenClaw tools.profile for the run (minimal | coding | messaging | full)"
        required: false
        default: "minimal"
      agent_prompt:
        description: "Override the agent prompt. For calibrating compliance without a code change; blank uses the default."
        required: false
        default: ""

permissions:
  contents: read

concurrency:
  group: openclaw-upstream-compat
  cancel-in-progress: false

env:
  QVAC_SMOKE_MODEL: qwen3.5-0.8b
  # Must stay under OPENCLAW_AGENT_TIMEOUT_SECONDS: the readiness wait happens
  # inside the agent run, so a readyTimeoutMs longer than the agent timeout can
  # never be reached -- the agent timeout fires first and reports an opaque kill
  # instead of the plugin's "service never became healthy" error. Observed
  # readiness is ~20-30s including the model download, so 3m is generous.
  QVAC_READY_TIMEOUT_MS: "180000"
  # Passed to `openclaw agent --timeout`, so a deadline arrives as a readable
  # JSON envelope rather than a SIGTERM. A healthy attempt on the bounded
  # surface is ~80s wall (run 34373947615); the pre-fix regime needed ~190s per
  # turn purely to prefill 36 tool schemas.
  OPENCLAW_AGENT_TIMEOUT_SECONDS: "240"

jobs:
  smoke:
    name: openclaw@latest + @qvac/openclaw-plugin@latest
    runs-on: ubuntu-latest
    # PR runs skip the agent turn (SKIP_OPENCLAW_AGENT below) so 15m covers
    # install + plugin setup. Scheduled/dispatch runs need install and setup
    # (~6m observed) plus the attempt budget, and the budget has to fit: six
    # attempts against the 300s backstop is 30m, so 36m is the worst case.
    # 45m leaves margin without letting a wedged run burn a full hour. Raising
    # `attempts` past 6 on a dispatch can exceed this -- the job timeout kills
    # the run before the artifact upload, so raise this too if you do.
    timeout-minutes: ${{ github.event_name == 'pull_request' && 15 || 45 }}
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2

      - name: Setup Node
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # 6.3.0
        with:
          node-version: lts/*

      - name: Run OpenClaw through QVAC plugin
        env:
          SMOKE_DIR: ${{ runner.temp }}/openclaw-compat-smoke
          ARTIFACT_DIR: ${{ runner.temp }}/openclaw-compat-artifacts
          QVAC_MODEL: ${{ env.QVAC_SMOKE_MODEL }}
          QVAC_READY_TIMEOUT_MS: ${{ env.QVAC_READY_TIMEOUT_MS }}
          OPENCLAW_AGENT_TIMEOUT_SECONDS: ${{ env.OPENCLAW_AGENT_TIMEOUT_SECONDS }}
          OPENCLAW_AGENT_MAX_ATTEMPTS: ${{ inputs.attempts || '6' }}
          OPENCLAW_TOOL_PROFILE: ${{ inputs.tool_profile || 'minimal' }}
          # Blank falls through to the script's default, so scheduled runs are
          # unaffected. The script must therefore treat empty as unset.
          AGENT_PROMPT: ${{ inputs.agent_prompt }}
          SKIP_OPENCLAW_AGENT: ${{ github.event_name == 'pull_request' && '1' || '0' }}
        run: bash scripts/ci/openclaw-upstream-compat-smoke.sh

      - name: Upload smoke artifacts
        if: always()
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # 7.0.0
        with:
          name: openclaw-upstream-compat
          path: ${{ runner.temp }}/openclaw-compat-artifacts
          if-no-files-found: error

  notify-on-failure:
    name: Open or update tracking issue
    needs: smoke
    # Manual runs are for debugging this workflow. Scheduled failures are the
    # unattended signal that should create team-visible follow-up.
    if: failure() && github.event_name == 'schedule'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # 6.0.2

      - name: Open or update tracking issue
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # 8.0.0
        with:
          script: |
            const title = 'OpenClaw upstream compatibility smoke failing';
            const label = 'github_actions';
            const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const body = [
              `Scheduled OpenClaw upstream compatibility smoke failed.`,
              ``,
              `- Run: ${runUrl}`,
              `- Artifact: \`openclaw-upstream-compat\``,
              `- Triage: read \`prompt-surface.md\` **first** — it reports the advertised tool count and prompt size. A count above the ceiling means upstream widened \`tools.profile\`, which is the failure this tripwire exists to catch.`,
              `- Then diff \`versions.md\` against the last passing run's artifact. If no package version moved, this is not upstream drift — look at \`openclaw-agent.stderr\` (turn count, \`failover decision\` lines), \`sessions/\`, and \`qvac-serve.stdout\`/\`qvac-serve.stderr\`.`,
              `- Possible causes, in the order worth checking: a widened tool surface (see above); a version bump visible in \`versions.md\`; a wedged \`qvac serve\` that stops answering mid-run; or model nondeterminism — the smoke drives a 0.8b model and retries up to three times before failing.`,
              `- To reproduce cheaply, dispatch this workflow with \`attempts: 1\`; \`tool_profile\` switches the surface back to \`coding\` if you need the pre-#4112 regime.`,
            ].join('\n');

            const issues = await github.paginate(github.rest.issues.listForRepo, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: label,
              per_page: 100,
            });
            const existing = issues.find((issue) => issue.pull_request === undefined && issue.title === title);

            if (existing !== undefined) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: existing.number,
                body,
              });
              core.info(`Appended comment to existing issue #${existing.number}`);
            } else {
              const created = await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
                labels: [label],
              });
              core.info(`Opened new issue #${created.data.number}`);
            }
