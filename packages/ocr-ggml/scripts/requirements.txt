# Python dependencies for scripts/pth_to_gguf.py
#
# Install with:
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r scripts/requirements.txt
#
# Easiest path: reuse the upstream EasyOCR venv (it already has torch + easyocr
# pinned to the versions used to produce the reference checkpoints):
#   ../EasyOCR/.venv/bin/python scripts/pth_to_gguf.py <in.pth> <out.gguf>

torch>=2.0
numpy>=1.24
gguf>=0.10.0
easyocr>=1.7.0
