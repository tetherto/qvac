# Changelog

## 0.1.0

- Add the managed `ggml-rpc-server` API, using a child process on Node and an
  in-process Bare addon on Android/iOS.
- Build prebuild artifacts for macOS arm64/x64, Linux arm64/x64, Windows x64, Android arm64, and iOS arm64.
