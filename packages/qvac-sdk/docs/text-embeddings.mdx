---
title: SDK — Text embeddings
description: Text embeddings via `llama.cpp` — i.e., use models to generate vector embeddings output from document inputs, for RAG and text similarity searches.
---

# Text embeddings

Text embeddings via `llama.cpp` — i.e., use models to generate vector embeddings output from document inputs, for RAG and text similarity searches.

## Usage

```ts file=../examples/embed-hyperdrive.ts

```
