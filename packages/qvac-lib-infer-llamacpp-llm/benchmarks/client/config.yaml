model_config:
  device: "gpu"  # Options: gpu (default, uses best available), cpu, auto, mps, cuda
  gpu_layers: "99"
  ctx_size: "10240"
  temp: "0.7"
  top_p: "0.9"
  n_predict: "4096"

benchmark:
  num_samples: 100
  datasets:
    - gsm8k
    - mmlu
    - squad
    - arc
