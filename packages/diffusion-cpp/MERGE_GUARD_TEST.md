# Merge-guard test — single-package change

Synthetic single-package touch (diffusion-cpp) to exercise qvac-merge-guard on the
fully-consolidated line (legacy on-pr/prebuilds deleted; prebuilds-nx is the only
prebuild producer). Delete after the test.
