diff --git a/include/stable-diffusion.h b/include/stable-diffusion.h
index 1111111..2222222 100644
--- a/include/stable-diffusion.h
+++ b/include/stable-diffusion.h
@@ -388,12 +388,25 @@
 typedef struct upscaler_ctx_t upscaler_ctx_t;
 
+enum sd_upscaler_device_t {
+    SD_UPSCALER_DEVICE_AUTO = 0,
+    SD_UPSCALER_DEVICE_CPU = 1,
+    SD_UPSCALER_DEVICE_GPU = 2,
+};
+
 SD_API upscaler_ctx_t* new_upscaler_ctx(const char* esrgan_path,
                                         bool offload_params_to_cpu,
                                         bool direct,
                                         int n_threads,
                                         int tile_size);
+SD_API upscaler_ctx_t* new_upscaler_ctx_with_device(const char* esrgan_path,
+                                                    bool offload_params_to_cpu,
+                                                    bool direct,
+                                                    int n_threads,
+                                                    int tile_size,
+                                                    enum sd_upscaler_device_t device);
+SD_API int get_upscaler_backend_device(const upscaler_ctx_t* upscaler_ctx);
 SD_API void free_upscaler_ctx(upscaler_ctx_t* upscaler_ctx);
 
 SD_API sd_image_t upscale(upscaler_ctx_t* upscaler_ctx,
                           sd_image_t input_image,
                           uint32_t upscale_factor);
diff --git a/src/upscaler.cpp b/src/upscaler.cpp
index 1111111..2222222 100644
--- a/src/upscaler.cpp
+++ b/src/upscaler.cpp
@@ -12,6 +12,8 @@
     int n_threads;
     bool direct   = false;
     int tile_size = 128;
+    // Post-init truth for stats: 0 = CPU, 1 = GPU (matches qvac RuntimeStats backendDevice).
+    int actual_backend_device = 0;
 
     UpscalerGGML(int n_threads,
                  bool direct   = false,
@@ -23,19 +25,25 @@
 
     bool load_from_file(const std::string& esrgan_path,
                         bool offload_params_to_cpu,
-                        int n_threads) {
+                        int n_threads,
+                        sd_upscaler_device_t device_preference) {
         ggml_log_set(ggml_log_callback_default, nullptr);
 
-        // Runtime GGML backend selection (aligned with StableDiffusionGGML::init_backend
-        // from sd-generic-backend-init). System-ggml builds omit SD_USE_VULKAN / SD_USE_*,
-        // which previously compiled out all GPU branches and forced CPU-only upscaling.
+        sd_upscaler_device_t pref = device_preference;
         if (std::getenv("SD_CPU_ONLY")) {
             LOG_INFO("ESRGAN upscaler: SD_CPU_ONLY set - using CPU backend");
+            pref = SD_UPSCALER_DEVICE_CPU;
+        }
+
+        backend = nullptr;
+        if (pref == SD_UPSCALER_DEVICE_CPU) {
+            LOG_INFO("ESRGAN upscaler: CPU device requested");
             backend = ggml_backend_init_by_type(GGML_BACKEND_DEVICE_TYPE_CPU, NULL);
             if (!backend) {
-                LOG_ERROR("ESRGAN upscaler: CPU backend init failed under SD_CPU_ONLY");
+                LOG_WARN("ESRGAN upscaler: CPU init_by_type failed; trying ggml_backend_cpu_init");
+                backend = ggml_backend_cpu_init();
             }
-        } else {
+        } else if (pref == SD_UPSCALER_DEVICE_GPU) {
             backend = ggml_backend_init_by_type(GGML_BACKEND_DEVICE_TYPE_GPU, NULL);
             if (!backend) {
                 LOG_WARN("ESRGAN upscaler: no GPU backend in registry; falling back to CPU");
@@ -45,6 +53,17 @@
                 LOG_WARN("ESRGAN upscaler: CPU init_by_type failed; trying ggml_backend_cpu_init");
                 backend = ggml_backend_cpu_init();
             }
+        } else {
+            // SD_UPSCALER_DEVICE_AUTO
+            backend = ggml_backend_init_by_type(GGML_BACKEND_DEVICE_TYPE_GPU, NULL);
+            if (!backend) {
+                LOG_WARN("ESRGAN upscaler: no GPU backend in registry; falling back to CPU (auto)");
+                backend = ggml_backend_init_by_type(GGML_BACKEND_DEVICE_TYPE_CPU, NULL);
+            }
+            if (!backend) {
+                LOG_WARN("ESRGAN upscaler: CPU init_by_type failed; trying ggml_backend_cpu_init");
+                backend = ggml_backend_cpu_init();
+            }
         }
 
         if (!backend) {
@@ -52,7 +71,8 @@
             return false;
         }
 
-        if (ggml_backend_is_cpu(backend)) {
+        actual_backend_device = ggml_backend_is_cpu(backend) ? 0 : 1;
+        if (actual_backend_device == 0) {
             LOG_INFO("ESRGAN upscaler compute backend: CPU");
         } else {
             LOG_INFO("ESRGAN upscaler compute backend: GPU (accelerated)");
@@ -123,11 +143,12 @@
     UpscalerGGML* upscaler = nullptr;
 };
 
-upscaler_ctx_t* new_upscaler_ctx(const char* esrgan_path_c_str,
-                                 bool offload_params_to_cpu,
-                                 bool direct,
-                                 int n_threads,
-                                 int tile_size) {
+upscaler_ctx_t* new_upscaler_ctx_with_device(const char* esrgan_path_c_str,
+                                            bool offload_params_to_cpu,
+                                            bool direct,
+                                            int n_threads,
+                                            int tile_size,
+                                            sd_upscaler_device_t device) {
     upscaler_ctx_t* upscaler_ctx = (upscaler_ctx_t*)malloc(sizeof(upscaler_ctx_t));
     if (upscaler_ctx == nullptr) {
         return nullptr;
@@ -136,10 +157,11 @@
 
     upscaler_ctx->upscaler = new UpscalerGGML(n_threads, direct, tile_size);
     if (upscaler_ctx->upscaler == nullptr) {
+        free(upscaler_ctx);
         return nullptr;
     }
 
-    if (!upscaler_ctx->upscaler->load_from_file(esrgan_path, offload_params_to_cpu, n_threads)) {
+    if (!upscaler_ctx->upscaler->load_from_file(esrgan_path, offload_params_to_cpu, n_threads, device)) {
         delete upscaler_ctx->upscaler;
         upscaler_ctx->upscaler = nullptr;
         free(upscaler_ctx);
@@ -148,6 +170,22 @@
     return upscaler_ctx;
 }
 
+upscaler_ctx_t* new_upscaler_ctx(const char* esrgan_path_c_str,
+                                 bool offload_params_to_cpu,
+                                 bool direct,
+                                 int n_threads,
+                                 int tile_size) {
+    return new_upscaler_ctx_with_device(
+        esrgan_path_c_str, offload_params_to_cpu, direct, n_threads, tile_size, SD_UPSCALER_DEVICE_AUTO);
+}
+
+int get_upscaler_backend_device(const upscaler_ctx_t* upscaler_ctx) {
+    if (upscaler_ctx == nullptr || upscaler_ctx->upscaler == nullptr) {
+        return -1;
+    }
+    return upscaler_ctx->upscaler->actual_backend_device;
+}
+
 sd_image_t upscale(upscaler_ctx_t* upscaler_ctx, sd_image_t input_image, uint32_t upscale_factor) {
     return upscaler_ctx->upscaler->upscale(input_image, upscale_factor);
 }
