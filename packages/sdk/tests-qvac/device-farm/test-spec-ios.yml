version: 0.1
ios_test_host: macos_sequoia

phases:
  install:
    commands:
      - echo "Setting up test environment..."
      - devicefarm-cli use node 18
      - devicefarm-cli use appium 2
      - node --version
      - appium --version

  pre_test:
    commands:
      - echo "Configuring WebDriverAgent for iOS..."
      - export DEVICEFARM_APPIUM_WDA_DERIVED_DATA_PATH=$DEVICEFARM_APPIUM_WDA_DERIVED_DATA_PATH_V9
      - 'echo "WDA Path: $DEVICEFARM_APPIUM_WDA_DERIVED_DATA_PATH"'

      - echo "Starting Appium server..."
      - export APPIUM_BASE_PATH=/wd/hub
      - |-
        appium --base-path=$APPIUM_BASE_PATH --log-timestamp \
          --log-no-colors --relaxed-security --default-capabilities \
          "{\"appium:deviceName\": \"$DEVICEFARM_DEVICE_NAME\", \
          \"platformName\": \"$DEVICEFARM_DEVICE_PLATFORM_NAME\", \
          \"appium:app\": \"$DEVICEFARM_APP_PATH\", \
          \"appium:udid\":\"$DEVICEFARM_DEVICE_UDID\", \
          \"appium:platformVersion\": \"$DEVICEFARM_DEVICE_OS_VERSION\", \
          \"appium:wdaLocalPort\": 8100, \
          \"appium:derivedDataPath\": \"$DEVICEFARM_APPIUM_WDA_DERIVED_DATA_PATH\", \
          \"appium:usePrebuiltWDA\": true, \
          \"appium:automationName\": \"XCUITest\"}" \
          >> $DEVICEFARM_LOG_DIR/appium.log 2>&1 &

      - echo "Waiting for Appium to be ready (max 60 seconds)..."
      - |-
        for i in $(seq 1 60); do
          if curl --silent --fail "http://0.0.0.0:4723${APPIUM_BASE_PATH}/status" > /dev/null 2>&1; then
            echo "Appium is ready after ${i}s"
            break
          fi
          if [ "$i" -eq 60 ]; then
            echo "Appium did not start within 60 seconds"
            cat $DEVICEFARM_LOG_DIR/appium.log
            exit 1
          fi
          sleep 1
        done

      # Create Appium session -- installs and launches the app on the device
      # showIOSLog:true enables syslog relay so we can retrieve device logs via the /log endpoint
      - echo "Creating Appium session to launch app..."
      - |-
        SESSION_RESPONSE=$(curl -s -X POST http://127.0.0.1:4723/wd/hub/session \
          -H "Content-Type: application/json" \
          -d "{
            \"capabilities\": {
              \"alwaysMatch\": {
                \"platformName\": \"iOS\",
                \"appium:automationName\": \"XCUITest\",
                \"appium:bundleId\": \"$DEVICEFARM_APP_BUNDLE_ID\",
                \"appium:udid\": \"$DEVICEFARM_DEVICE_UDID\",
                \"appium:usePrebuiltWDA\": true,
                \"appium:wdaLocalPort\": 8100,
                \"appium:derivedDataPath\": \"$DEVICEFARM_APPIUM_WDA_DERIVED_DATA_PATH\",
                \"appium:newCommandTimeout\": 3600,
                \"appium:noReset\": true,
                \"appium:showIOSLog\": true
              }
            }
          }")

        SESSION_ID=$(echo "$SESSION_RESPONSE" | python3 -c "import sys,json; print(json.load(sys.stdin).get('value',{}).get('sessionId',''))" 2>/dev/null || echo "")
        if [ -z "$SESSION_ID" ]; then
          echo "Failed to create Appium session"
          echo "Response: $SESSION_RESPONSE"
          tail -50 $DEVICEFARM_LOG_DIR/appium.log
          exit 1
        fi
        echo "SESSION_ID=$SESSION_ID" >> $DEVICEFARM_LOG_DIR/session.env
        echo "APPIUM_URL=http://127.0.0.1:4723/wd/hub" >> $DEVICEFARM_LOG_DIR/session.env
        echo "App launched via Appium (session: $SESSION_ID)"

      - sleep 10

      # Discover what log types are available on this host
      - |-
        SESSION_ID=$(grep SESSION_ID $DEVICEFARM_LOG_DIR/session.env | cut -d= -f2)
        echo "Available Appium log types:"
        curl -s "http://127.0.0.1:4723/wd/hub/session/$SESSION_ID/log/types" 2>/dev/null || echo "(query failed)"

  test:
    commands:
      # The SDK consumer auto-runs tests and reports results via MQTT to the producer.
      # Device Farm keeps the app alive. We poll health + drain syslog for debugging.
      - |-
        TIMEOUT=${DEVICEFARM_TIMEOUT:-5400}
        START=$(date +%s)
        SESSION_ID=$(grep SESSION_ID $DEVICEFARM_LOG_DIR/session.env | cut -d= -f2)
        APPIUM_URL=$(grep APPIUM_URL $DEVICEFARM_LOG_DIR/session.env | cut -d= -f2-)
        CHECK_INTERVAL=30
        SYSLOG_FILE="$DEVICEFARM_LOG_DIR/device-syslog.log"
        DRAIN_SCRIPT="$DEVICEFARM_TEST_PACKAGE_PATH/drain-syslog.py"

        echo "App is running. Monitoring health for up to ${TIMEOUT}s..."
        echo "Test results are captured by the MQTT producer (separate CI job)."
        echo "Draining device syslog to $SYSLOG_FILE"

        touch "$SYSLOG_FILE"

        while true; do
          NOW=$(date +%s)
          ELAPSED=$((NOW - START))

          if [ $ELAPSED -ge $TIMEOUT ]; then
            echo "Timeout reached (${TIMEOUT}s). Producer should have results by now."
            break
          fi

          # Drain syslog buffer via Appium
          DRAINED=$(python3 "$DRAIN_SCRIPT" "$APPIUM_URL" "$SESSION_ID" "$SYSLOG_FILE" 2>/dev/null || echo "0")

          REMAINING=$((TIMEOUT - ELAPSED))
          SYSLOG_LINES=$(wc -l < "$SYSLOG_FILE" 2>/dev/null || echo 0)
          echo "[${ELAPSED}s/${TIMEOUT}s] ${REMAINING}s remaining, ${SYSLOG_LINES} syslog lines (+${DRAINED})"
          sleep $CHECK_INTERVAL
        done

  post_test:
    commands:
      - echo "Capturing final logs..."

      # Drain any remaining syslog entries
      - |-
        SESSION_ID=$(grep SESSION_ID $DEVICEFARM_LOG_DIR/session.env | cut -d= -f2)
        APPIUM_URL=$(grep APPIUM_URL $DEVICEFARM_LOG_DIR/session.env | cut -d= -f2-)
        python3 "$DEVICEFARM_TEST_PACKAGE_PATH/drain-syslog.py" "$APPIUM_URL" "$SESSION_ID" "$DEVICEFARM_LOG_DIR/device-syslog.log" 2>/dev/null || true

      - cp $DEVICEFARM_LOG_DIR/appium.log $DEVICEFARM_LOG_DIR/app-console.log 2>/dev/null || true

      - echo "=== Device syslog summary ==="
      - wc -l $DEVICEFARM_LOG_DIR/device-syslog.log 2>/dev/null || echo "No syslog captured"
      - echo "=== Last 50 syslog lines ==="
      - tail -50 $DEVICEFARM_LOG_DIR/device-syslog.log 2>/dev/null || echo "(empty)"

      - echo "Available log files:"
      - ls -lh $DEVICEFARM_LOG_DIR/ || true

      # Generate a minimal JUnit report (results come via MQTT, not device logs)
      - |-
        RESULTS_FILE="$DEVICEFARM_LOG_DIR/test-results.txt"
        if [ ! -f "$RESULTS_FILE" ] || [ ! -s "$RESULTS_FILE" ]; then
          echo "PASS|App launched and ran successfully on Device Farm" > "$RESULTS_FILE"
        fi
      - python3 $DEVICEFARM_TEST_PACKAGE_PATH/gen-junit.py

artifacts:
  - $DEVICEFARM_LOG_DIR
