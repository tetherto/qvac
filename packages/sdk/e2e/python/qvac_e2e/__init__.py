"""Python e2e client for the shared QVAC test catalog.

Interprets declarative test definitions handed over the framework's
stdin/stdout bridge, so the Python SDK runs the same catalog the JS legs run
rather than a hand-maintained copy of it.
"""
