// Public `@qvac/sdk/plugin-utils` subpath. The plugin definition helpers, types,
// and specifier constants live in core; the SDK re-exports them under its own
// subpath so consumers keep importing from `@qvac/sdk/plugin-utils`.
export * from '@qvac/core/plugin-utils'
