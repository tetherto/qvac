# DRYRUN — DELETE ME

Temporary marker so a `packages/tts-ggml/**` path change triggers the on-pr
flow during dry-run validation of the prebuild-status fail-closed fix
(PR #3779). This file and the `pull_request` dry-run wiring are deleted before
merge.
