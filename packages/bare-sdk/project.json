{
  "name": "@qvac/bare-sdk",
  "root": "packages/bare-sdk",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm run build",
        "cwd": "packages/bare-sdk"
      }
    }
  }
}
