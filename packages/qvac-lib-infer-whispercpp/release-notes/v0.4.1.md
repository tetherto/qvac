# QVAC Transcription Whisper Addon v0.4.1 Release Notes

This release removes the Hyperdrive dependency for model distribution in favor of downloading standard Whisper models directly from HuggingFace. Fine-tuned models maintained by the team are now served via S3. New download scripts and automatic model fetching in test helpers simplify getting started.

## Breaking Changes

### Removed Hyperdrive Dependency

The `@qvac/dl-hyperdrive` peer dependency and dev dependency have been removed. All references to Hyperdrive-based model loading, including the Hyperdrive key constant and `HyperDriveDL` loader, have been stripped from the test helpers and integration tests.

If you were relying on `@qvac/dl-hyperdrive` being re-exported from the test helpers or using the `WHISPER_MODEL_HYPERDRIVE_KEY` constant, those are no longer available. Models are now expected to be pre-downloaded to disk or automatically fetched from HuggingFace by the test infrastructure.

### Removed Hyperdrive Examples

The following example files have been removed:

- `examples/transcription.hd.js`
- `examples/exampleVad.hd.js`

Use `examples/quickstart.js` or `examples/example.live-transcription.js` instead, which use the filesystem data loader.

## Features

### HuggingFace Model Downloads

Standard Whisper models and the Silero VAD model are now downloaded directly from their public HuggingFace repositories:

- Whisper models from [ggerganov/whisper.cpp](https://huggingface.co/ggerganov/whisper.cpp) (tiny through large-v3-turbo, including quantized variants)
- VAD model from [ggml-org/whisper-vad](https://huggingface.co/ggml-org/whisper-vad) (ggml-silero-v5.1.2.bin)

The test helpers (`ensureWhisperModel` and `ensureVADModel`) now automatically download models from HuggingFace when they are not found locally, using `curl`. This means integration tests will self-provision their required models on first run.

### Download Scripts

Two new scripts are available under `scripts/`:

- **`download-models.sh`** -- Interactive script for downloading standard Whisper models from HuggingFace. Run it via `npm run download-models`. Supports individual model selection, quantized variants, and bundle downloads (e.g., tiny + VAD for development).
- **`download-models-s3.sh`** -- Downloads fine-tuned GGML models maintained by the team from S3. Requires AWS credentials and `MODEL_S3_BUCKET` environment variable.

### Architecture Documentation

New documentation files have been added covering the internal architecture and detailed data flows of the transcription addon.

## Pull Requests

- [#398](https://github.com/tetherto/qvac/pull/398) - docs: add architecture documentation for qvac-lib-infer-whispercpp
- [#428](https://github.com/tetherto/qvac/pull/428) - docs: Added architecture document for tts package
