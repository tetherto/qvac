# Downloaded models (large GGUF files) — fetched on demand by
# `npm run prepare:models`. Never commit them.
models/

# Per-run output: aggregated JSON, Markdown report, per-cell stderr
# logs, and the spec/result JSONs the orchestrator passes to bare.
results/

# Generated by prepare-models.js after model resolution.
resolved-models.json
