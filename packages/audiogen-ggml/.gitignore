# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
build/
build_*/
build-*/
dist/
out/
*.o
*.so
*.dylib
*.dll
*.exe

# Prebuilds (platform-specific binaries)
prebuilds/

# vcpkg
_vcpkg/
vcpkg_installed/

# CMake build files
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

# Downloaded ACE-Step GGUFs (multi-GB; fetched from the model registry)
models/

# Benchmark run output (uploaded as CI artifacts, never committed).
# benchmarks/manual-results/ is tracked: it is the drop point for backends CI
# cannot cover.
benchmarks/results/

# Local generation and comparison audio (never committed)
output/

# IDE and editor files
.vscode/
.idea/
.DS_Store
