@AGENTS.md

## Claude Code

Run `/setup claude` after cloning to create the local, gitignored Claude skill
compatibility view. Re-run it after skill changes on Windows, where setup uses
copies instead of symlinks. Edit canonical skills in `.agents/skills`, not under
`.claude/skills`.
