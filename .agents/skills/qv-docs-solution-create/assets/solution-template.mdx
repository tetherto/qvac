---
title: <Recurring scenario or task the page covers>
description: <One sentence that qualifies the scenario so a reader can judge relevance.>
---

{/* Remove this block before publishing.

    Required: Scenario, Recommended approach.
    Then use headings drawn from this case; there is no mandatory Implementation heading.
    Considerations, Tested with, and Related resources are standardized but optional — delete any of them rather than shipping it empty.

    Full rules: references/editorial-contract.md */}

## Scenario

<The situation before a solution was chosen: goal, environment, infrastructure, existing software, integration context, requirements, and constraints. Prose and bullets as they fit; no fixed subheadings.>

## Recommended approach

<The strategy and its rationale: main approach, structural decisions, division of responsibilities, boundaries. What to do and why, not yet how.>

## <Case-specific heading>

<How the approach works or is applied in practice. Name the subject in the heading — `Container requirements`, `API compatibility`, `Security boundaries` — never `Details` or `Implementation details`.>

## Considerations

<Tradeoffs, limitations, edge cases, pitfalls, operational concerns, security implications, alternatives, non-obvious behavior.>

## Tested with

This solution was tested with:

- <version, hardware, model, quantization, or other element that was exercised>

<Add the measurements or a results table when they exist. Keep recommendation and proven behavior distinguishable, and adapt the lead-in sentence when the content calls for something else.>

## Related resources

<What the reader needs next, inside or outside the docs website: canonical QVAC pages as absolute site-root paths such as `/cli/http-server`, plus public external material the Solution depends on — a package, a repository, a public issue, a model card, a third-party product's documentation. Summarize only what the Solution needs and link the rest.>
