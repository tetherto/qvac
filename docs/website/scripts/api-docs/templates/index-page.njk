{#- Render the API index page.
    Receives: { functions: ApiFunction[], versionLabel: string }
    Delegates to the renderIndexPage() global for byte-identical output
    with the former generateIndexMDX(). -#}
{{ renderIndexPage(functions, versionLabel) }}
