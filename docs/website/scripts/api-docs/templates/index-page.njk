{#- Render the API index page.
    Receives: { functions: ApiFunction[], objects?: ApiObject[], versionLabel: string }
    Filters: firstSentence -#}
---
title: "@qvac/sdk"
titleStyle: code
description: API reference — {{ versionLabel }}
---

## Overview

`@qvac/sdk` npm package exposes a function-centric, typed JS API.

## Functions

| Function | Summary |
| --- | --- |
{% for fn in functions %}
| [`{{ fn.name }}()`](./{{ fn.name }}) | {{ fn.description | firstSentence | replace("|", "\\|") }} |
{% endfor %}

{% if objects and objects.length > 0 %}
## Object

| Object | Summary |
| --- | --- |
{% for obj in objects %}
| [`{{ obj.name }}`](./{{ obj.name }}) | {{ obj.description | firstSentence | replace("|", "\\|") }} |
{% endfor %}

{% endif %}
## Shared types

<Card href="./shared-types" title="Shared types">
  Common type definitions used across SDK functions.
</Card>

## Errors

<Card href="./errors" title="Error codes">
  All errors thrown by the SDK and how to handle them via `SDK_CLIENT_ERROR_CODES` and `SDK_SERVER_ERROR_CODES`.
</Card>
