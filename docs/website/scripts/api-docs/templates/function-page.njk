{#- Render a single ApiFunction as an MDX page.
    Receives: { fn: ApiFunction }
    Delegates to the renderFunctionPage() global for byte-identical output
    with the former generateMDXForFunction(). -#}
{{ renderFunctionPage(fn) }}
