{#- API index page with function and object summary tables.
    Context: { functions: ApiFunction[], objects: ApiObject[], versionLabel: string }
-#}
---
title: "@qvac/sdk"
titleStyle: code
description: API reference — {{ versionLabel }}
---

## Overview

`@qvac/sdk` npm package exposes a function-centric, typed JS API.

## Functions

| Function | Summary |
| --- | --- |
{% for fn in functions -%}
| [`{{ fn.name }}()`](./{{ fn.name }}) | {{ fn.description | firstSentence | escapeTable }} |
{% endfor %}
{% if objects and objects.length > 0 -%}
## Object

| Object | Summary |
| --- | --- |
{% for obj in objects -%}
| [`{{ obj.name }}`](./{{ obj.name }}) | {{ obj.description | firstSentence | escapeTable }} |
{% endfor %}
{% endif -%}
## Errors

<Card href="./errors" title="Error codes">
  All errors thrown by the SDK and how to handle them via `SDK_CLIENT_ERROR_CODES` and `SDK_SERVER_ERROR_CODES`.
</Card>
