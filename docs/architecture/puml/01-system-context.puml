@startuml
!include <C4/C4_Context>

title QVAC SDK Family - System Context

Person(dev, "Application Developer", "Builds JS, Python, Bare, desktop, and mobile apps")

System(sdk, "QVAC SDK Family", "Local-first P2P inference. JS, Python, and Bare entry points share one engine")

System_Ext(registry, "QVAC Model Registry", "Catalog, search, and Hyperblob download")
System_Ext(http, "HTTP / CDN", "Direct model file downloads")
System_Ext(p2p, "Holepunch P2P Network", "Hyperswarm DHT, Hyperdrive seeders, delegated inference providers")

Rel_D(dev, sdk, "Calls SDK API")
Rel_L(sdk, registry, "Catalog queries and downloads")
Rel_D(sdk, http, "HTTP model downloads")
Rel_R(sdk, p2p, "Peer discovery, P2P model sync, delegated inference")

@enduml
