@startuml
title RPC Communication Flow

participant "Client" as Client
participant "Transport" as Transport
participant "Engine" as Engine

Client -> Transport : completion({ modelId, history })
Transport -> Engine : JSON request

loop Token streaming
    Engine --> Transport : token chunk
    Transport --> Client : token chunk
end

Engine -> Transport : final { stats }
Transport -> Client : final { stats }

@enduml
