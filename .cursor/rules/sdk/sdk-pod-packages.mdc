---
description: Resolve current SDK pod ownership and release package slugs
alwaysApply: false
---

# SDK pod scope

Read `.github/teams/sdk.json` for current ownership and
`scripts/sdk/package-paths.cjs` for release/changelog package slugs. Do not maintain
a duplicate package roster in agent configuration.
