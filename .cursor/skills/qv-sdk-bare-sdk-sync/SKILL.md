# Deprecated: use `/qv-sdk-lockstep-sync`

This skill was renamed and expanded to cover all lockstep SDK clients
(`@qvac/bare-sdk` and `tetherto-qvac-sdk`).

**Read and follow:** `.cursor/skills/qv-sdk-lockstep-sync/SKILL.md`

Legacy script path (still works; forwards to the new location):

```bash
node .cursor/skills/qv-sdk-bare-sdk-sync/scripts/sync.mjs
```

Prefer:

```bash
node .cursor/skills/qv-sdk-lockstep-sync/scripts/sync-bare-sdk.mjs
```
